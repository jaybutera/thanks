use anyhow::Result;
use crate::grammar::types::RawAst;
use crate::types::Thesis;

/*
pub fn compile(raw: RawAst) -> Result<Vec<Thesis>> {
    let ast_thunks = raw.theses.iter()
        .map(|thesis| thesis.thunks

    let mut thunks = vec![];
    for thesis in raw.theses {
        for thunk in thesis.thunks {
            thunks.push(Thunk {
                refs: 
                text: thunk.text,
            })
        }
    }
}
*/
